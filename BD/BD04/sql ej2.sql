select rider.name, rider.age from rider /*selects the rider name and the rider age from the table rider*/
order by age DESC; /*orders the selectes by the age of the riders, with the oldest first */
